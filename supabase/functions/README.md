-- Deno Edge Function stub: AI generation proxy (Phase 2)
-- Secrets (OPENAI_API_KEY etc.) must be set via `supabase secrets`, never in the client.

-- Placeholder TypeScript entry lives beside this comment when Phase 2 begins:
-- supabase/functions/ai-generate/index.ts
